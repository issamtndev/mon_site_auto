{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Modele list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Marque</th>
                <th>Modele</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('modele_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.marques.nom }}</td>
                <td>{{ entity.nom }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('modele_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('modele_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('modele_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
